import heapq
T = int(input())
for test_case in range(1, T + 1):
    n,m=input().split()
    m=int(m)
    n=[i for i in n]
    q=[]
    visited=[False]*len(n)
    def arr(current,index,remain,visited):
        if remain==0
            heapq.heappush(-int(current))

    print(f"#{test_case} {('').join(n)}")